name: CI / Deploy (Contracts -> Verify -> Vercel)
on:
  push:
    branches: [ main ]
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 18
      - name: Placeholder
        run: echo "Configure secrets and deploy"
